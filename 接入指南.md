# TD iOS SDK 接入指南（三步）

完整对接文档：https://tydeonj.github.io/td-sdk-docs/  
Android Demo：https://github.com/tydeonj/td-sdk-demo-android

广告位只改 [`TDAdsSDKDemo/TDAdsSDKDemo/AdConfig.h`](TDAdsSDKDemo/TDAdsSDKDemo/AdConfig.h)（本 Demo：`appId=31016`，开屏 621301 / 激励 621305 / 插屏 621304 / Banner 621302 / 原生 621303）。Android 对外是另一套：`appId=31014` / 插屏 621314 / Banner 621311 / 开屏 621313 / 原生 621312 / 激励 621315。

---

## 1. 集成

正式工程用 CocoaPods Trunk。Other Linker Flags 加 `-ObjC`。没接的平台不要加对应 pod。本 Demo 的 `Podfile` 已按 Trunk `1.1.2.6` 拉这五个模块。完整对照见 [03 iOS 集成](https://tydeonj.github.io/td-sdk-docs/03_iOS集成)。

```ruby
platform :ios, '12.0'
use_frameworks! :linkage => :static

target 'YourApp' do
  pod 'TDAdsBase', '1.1.2.6'
  pod 'TDAdsSDK',  '1.1.2.6'
  # 按需：TD 模块 + 官方 SDK 都要加（与 Android 相同）
  # pod 'TDAdsJDSDK',  '1.1.2.6'
  # pod 'JinDaiSDK', :subspecs => ['JinDaiSDK']
  # pod 'TDAdsAdGain', '1.1.2.6'
  # pod 'AdGainSDK', '4.2.8.2'
  # Trunk 未收录时：先拉 Gitee tag 4.2.8.2，再 pod 'AdGainSDK', :path => 'path/to/AdGainSDK'
  # pod 'TDAdsLtmb',   '1.1.2.6'
  # pod 'LitemobSDK', '~> 5.5'
end
```

| 平台 | TD 模块 | 官方 SDK | 何时加 |
|------|---------|----------|--------|
| （无） | `TDAdsBase` + `TDAdsSDK` | — | 必加 |
| JinDai | `TDAdsJDSDK` | `JinDaiSDK` | 后台配了 JinDai |
| AdGain | `TDAdsAdGain` | `AdGainSDK` `4.2.8.2` | 后台配了 AdGain |
| LiteMob | `TDAdsLtmb` | `LitemobSDK`（检查 Embed & Sign；纯 ObjC 加空 `Dummy.swift`；宿主 Deployment Target ≥ 12.2；**关闭** Always Embed Swift Standard Libraries，rpath 优先 `/usr/lib/swift`） | 后台配了 LiteMob |

`pod install` 后用 `.xcworkspace` 打开。`Info.plist` 里的 `SKAdNetworkItems` 是模板，上线前只合并已接平台的官方列表。

---

## 2. Init

本 Demo 首页不展示合规开关。点「初始化 SDK」会按默认同意调用隐私 API，方便看广告流程。

**正式 App 必须先让用户同意自己的隐私政策，再 Init。** 对照 https://tydeonj.github.io/td-sdk-docs/隐私合规。

```objc
#import <TDAdsSDK/TDAdsSDK.h>
#import <TDAdsSDK/TDInitListener.h>

[TDAdsSDK setDebugMode:YES]; // 对外调试开关，给接入方开发者调用；正式上线请关
[TDAdsSDK setAuthUID:YES]; // 允许采 IDFA；SDK 默认关。init 前申请 ATT，Info.plist 配 NSUserTrackingUsageDescription；SDK 不代弹。JinDai / AdGain 无 IDFA 不出广告，见 [04](https://tydeonj.github.io/td-sdk-docs/04_初始化与隐私)
[TDAdsSDK setOpenPersonalizedAd:YES]; // 默认开、不持久化
[TDAdsSDK initSdkWithAppId:@"2" listener:self];
```

Init 成功后控制台搜 `TD integration check`（不铺在首页）：

- `adapter … LINKED`：TD 桥已编进包
- `peer … LINKED`：对方 SDK 入口类已链上
- `missing`：没加对应 xcframework / 对方包。这是 no_fill 之前先看的

`setDebugMode(true)` 后，某个源的加载搜它自己的类名。控制台搜 `TDJdsdkReward` 能看到 `bind` / `load start` / `load ok|fail` 就是进了京东激励 Adapter；**一条没有** = Config 没下发 / className 对不上 / 模块没编进包。Init 搜 `TDJdsdkInit`。其它源同理：`TDAdgainReward`、`TDLtmbReward`。Android 是类短名、无 `TD` 前缀，滤 `JdsdkReward`。

---

## 3. 按样式抄文件

五种样式在一个 [`FormatViewController.m`](TDAdsSDKDemo/TDAdsSDKDemo/FormatViewController.m) 里，按 `type` 分支，对齐 Android `FormatActivity`。抄对应 `createAd` / `load` / `show` 段，把广告位换成自己的。完整 API 表与示例见 [05](https://tydeonj.github.io/td-sdk-docs/05_激励视频)～[09](https://tydeonj.github.io/td-sdk-docs/09_原生广告)。

| type | 广告 | 类 |
|------|------|-----|
| `reward` | 激励 | `TDReward` |
| `interstitial` | 插屏 | `TDInterstitial` |
| `splash` | 开屏 | `TDSplash`（Load 前 `setContainer`） |
| `banner` | Banner | `TDBanner` |
| `native` | 原生 | `TDNative`（Load 不自动 Show；自渲染在点 Show 时拼装） |

容器必须按格式抄（否则 ADN 拒 Show，Demo 看不见广告）：

| type | 容器 |
|------|------|
| `splash` | 全屏 overlay，不要 120pt 小盒子；用 `alpha=0` 占位 |
| `banner` | 宽撑满、高 ≥50pt（京东自适应，建议 200pt，锁 50pt 会压扁） |
| `native` | ≥250pt；自渲染在 Show **之前**拼 `td_title` / `td_desc` / `td_cta` / `td_image`（`imageUrl`）。`UILabel` 须 `userInteractionEnabled = YES`。SDK Load 成功不会自动 Show |
| `reward` / `interstitial` | 无容器 |

共通顺序：`initWithAdUnitId` → `setAdListener` → `loadAd` → `isReady` → `showAdFrom:sceneId:`（开屏 / Banner / 原生再带 `container`）。离开页面调 `onDestroy`。

请求尺寸：未传时用格式默认（Banner 屏宽×50pt，原生 250pt，开屏整屏）。要改在 Load 前（**像素 / pt**）：

```objc
[banner setAdSize:320 height:50];     // 或
[banner setCustomParams:@{
    @"width": @320,
    @"height": @50
}];
```

容器仍要够大，否则 ADN 拒 Show。

激励走 LTMB / `LMAdSDK` 时，Load 前必须带用户 ID（飞鸽展示可能要求非空）：

```objc
[reward setCustomParams:@{ @"userId": @"your_app_user_id", @"channel": @"your_channel" }];
```

正式包请换成你们的登录用户 ID。详见 [10 自定义参数与尺寸](https://tydeonj.github.io/td-sdk-docs/10_自定义参数与尺寸)。`sceneId` 会进 TD 埋点；若对接平台要求数字，请传纯数字串。
